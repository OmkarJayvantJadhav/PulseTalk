# Core FastAPI server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Pydantic
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0

# Hugging Face stack (pinned to versions with Python 3.13 wheels)
transformers>=4.46.0,<5.0
tokenizers>=0.20.0
huggingface-hub>=0.26.0
safetensors>=0.4.5

# PyTorch CPU-only build (required for Render free instances)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.11.0+cpu

# HTTP client (used by FastAPI TestClient and any outbound calls)
httpx>=0.27.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
